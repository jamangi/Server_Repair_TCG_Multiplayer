# TASK-055 visual evidence

These reference captures are produced by
`tests/browser/task-055-system-model-view.spec.mjs` when
`UPDATE_TASK_055_VISUALS=1` is set. They are review evidence, not golden-image
assertions.

The desktop capture records the semantic and focus-restoration scenario. The
project/scale captures record the responsive, touch, reduced-motion, and
forced-colors matrix described in
`docs/system-models/task-055/BROWSER_QA.md`.
