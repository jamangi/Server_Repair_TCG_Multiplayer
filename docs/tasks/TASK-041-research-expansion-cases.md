# TASK-041-XHIGH: Research the expansion case set

## Status

**Completed 2026-08-28.** Depends on completed TASK-040.

## Objective

Apply the approved expansion protocol through its research gate. Select uncovered existing learning arcs, compute Q, then find and reduce the minimum Q qualifying real-world post-market server-repair cases needed to support six distinct expansion episodes.

## Requirements

Read `AGENTS.md`, this task, TASK-004, TASK-039, TASK-040, all case-study artifacts and current domain/playable catalogs completely before research. Search current primary or firsthand sources; directly open every selected source; preserve access date, locator, eligibility, uncertainty, rejected alternatives, and brief copyright-safe paraphrase.

- Begin with the 72-case v0.1 backlog and current research drafts before opening a parallel inventory.
- Count an existing uncovered arc only if it has a complete truthful Symptom→Fault→diagnostic evidence→Repair→Verify path and can support a distinct solvable Match.
- Compute `Q = max(0, six expansion slots - validated distinct existing arcs)`, then adjust only when one case cannot supply the missing lifecycle or subsystem diversity. Explain every adjustment.
- For each selected case, use the Observe/Hypothesize/Test/Isolate/Repair/Verify/Document rubric and cross-reference every phrase to current stable objects.
- Separate exact matches, generic equivalents, aliases, educational detail, missing objects, and possible mechanics. Never promote a vendor alias or incidental tool into a new object automatically.
- Produce a six-slot evidence matrix showing which slots are already supported and which depend on candidate additions. Record Commands encountered and their real diagnostic contribution without changing game semantics.

## Deliverables

Create a versioned case-study expansion set, source ledger, lifecycle reductions, found/not-found cross-references, deduplicated candidate objects, relationship-impact candidates, Q calculation, six-slot evidence matrix, and research-completion report. Update the case-study README and registry without rewriting v0.1 history.

## Verification and boundaries

Validate URLs, stable IDs, unique case/pair identities, one lifecycle category per step, explicit/inferred fidelity, source eligibility, candidate deduplication, Q arithmetic, and all documentation links. Run `git diff --check`.

This task is research only. Do not add domain IDs, Cards, fingerprints, Tickets, Story nodes, dialogue, art, or rules. Stop and record a blocker rather than weaken source eligibility or fabricate a complete lifecycle.
