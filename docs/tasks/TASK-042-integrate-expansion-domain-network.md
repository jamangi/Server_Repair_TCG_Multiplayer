# TASK-042-XHIGH: Integrate the approved expansion domain network

## Status

**Planned.** Depends on TASK-041 and any concrete rule/schema approvals it exposes.

## Objective

Convert only the reviewed TASK-041 candidates needed for the six expansion episodes into a coherent, playable domain network with complete provenance, relationships, Cards, fingerprints/Tickets, active-deck reachability, and deterministic solvability.

## Requirements

Read `AGENTS.md`, this task, TASK-009, TASK-014, TASK-024, TASK-039 through TASK-041, selected case artifacts, schemas, authoring guides, frozen/unfrozen decisions, and all generators/validators completely.

- Reuse existing stable objects when technically sufficient; add the smallest non-duplicative set.
- For every addition, trace source case, lifecycle role, schema, relations in/out, playable projection, technical copy, acronym/glossary needs, illustration need, and migration/version effect.
- Update the whole affected network: Symptoms, Faults, causal relationships, Tests/Commands, Repairs, Validations, Components, Tools, Protocols, Cards, diagnostic outcomes, candidate sets, fingerprints/Tickets, deck/resource requirements, and coverage reports as applicable.
- Reject orphan objects, one-way links, incompatible outcomes, hidden-information leaks, unsupported causality, and unplayable actions.
- Commands may become useful or required only through an approved semantic contract and authored evidence relationships. Do not pressure Tests into Commands or change frozen Isolation authority to satisfy a coverage target.
- Prove each proposed episode path with the real Builder/engine and prove existing content remains stable or migrates explicitly.

## Verification and boundaries

Run all schema/reference/content generators, technical-copy/provenance checks, art coverage checks, Builder solvability/replay proofs, deterministic campaign reports, staged Viewer verification, baseline/browser tests affected by content, and `git diff --check`. Report new/reused/rejected object counts and relationship deltas.

Do not design the expansion graph or dialogue. Stop when the six-slot evidence matrix has domain-backed, deck-reachable, solvable candidate Match inputs and no unresolved authority question is hidden in content.
